<div class="container-fluid p-0">
  <div class="row">
    <div class="col-12">
      <h1 class="h3"><%= @topping.name %></h1>
      <hr />
      <ul class="nav nav-pills justify-content-end">
        <li class="nav-item">
          <%= link_to 'Edit', edit_topping_path(@topping), class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Delete', topping_path(@topping), class: 'nav-link', data: {
                                    turbo_method: :delete,
                                    turbo_confirm: 'Are you sure?'
                                  } %>
          </li>
        <li class="nav-item"><%= link_to 'List', toppings_path, class: 'nav-link' %></li>
      </ul>
    </div>
  </div>
</div>
